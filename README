This is a ruby project implementing a game of hangman

To run, simply run `./hangman`

To run in a different language, run `LANGUAGE=fr ./hangman`. Currently supported languages are french (fr), and spanish (es).

To run the tests, simply run `rspec`

### Copyright

©2015 Powershop New Zealand Limited 
